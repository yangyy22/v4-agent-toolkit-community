{
  "name": "@agent-toolkit/client-harness",
  "version": "0.1.0",
  "private": true,
  "description": "Benchmark harness: MCP client that consumes every server declared in the repository-root mcp.json",
  "type": "module",
  "scripts": {
    "run": "tsx runner.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "tsx": "^4.19.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
